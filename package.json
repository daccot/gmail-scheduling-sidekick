{
  "name": "gmail-scheduling-sidekick",
  "version": "0.8.0",
  "private": true,
  "description": "A Gmail side panel assistant for scheduling candidates, reply templates, thread status, local memos, and Google Calendar draft holds.",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.3"
  }
}